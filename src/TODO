
PanoSalado.as:
- Show first frame of video without requiring playback.
- JavaScript access
- Refactor for easier maintenance

FlexUserInterface.mxml:
- Zoom control.
- Keyboard bindings for playback

General:
- Support interpreting XML from an image or stream metadata.
- Reduce file size.
- Improve playback frame rate when idle
